// Chinese source strings are the translation keys, so the default locale
// intentionally falls back to each key instead of duplicating the catalog.
export const zhCNMessages: Record<string, string> = {};
